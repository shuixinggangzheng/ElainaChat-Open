# ElainaChat v1.3.1 更新文档

> 双端同源：安卓 App（手机/平板）+ PC 客户端（Windows 10/11）
> 本文档覆盖从界面重构到当前的全部改动，含两轮代码审查的修复与三设备功能验证结果。

---

## 一、界面全面重构

- **液态玻璃视觉**：系统蓝（#007AFF）强调色 + 灰白玻璃底，光斑透出、镜面反射、厚度高光
- **4 套外观模板**：iOS 蓝（默认）/ 陶土橙 / 鼠尾草 / 樱花桃，各含浅色与深色变体，设置里点色板即切换
- **深色模式**：石墨黑 + 亮蓝光斑，一键切换并持久保存
- **Swift 风格设置**：顶部 Finder 分段导航（外观 / 连接 / 语音 / 记忆与日志 / 角色设定 / 桌宠 / Galgame），分组卡片 + 滑动切换，手机端自动换行完整显示
- **按钮与交互**：大圆角、立体感、按压回弹动画；修复任意分辨率下按钮文字错位
- **聊天区背景**：奶油灰渐变 + 网格 + 光斑，头部玻璃化透出模板色

## 二、Galgame 界面（新增）

- **全屏沉浸**：场景背景图 + 立绘 + 底部玻璃对话框（打字机）+ 输入行
- **30 张场景背景**：卧室/咖啡厅/游乐园/天鹅湖/雪夜街道/雪湖栈道等，设置里用缩略图横向选择，换场景双层交叉淡入
- **粒子特效**：随场景 / 无 / 星空 / 雨 / 雪 / 樱花瓣（夜晚图自动星空、雪景图自动飘雪）
- **AI 自主选背景**：AI 在回复末尾输出场景标记即自动切换（标记不会显示给用户，也不会进入朗读与记忆）
- **与主界面共用同一份对话数据**：两边说的话互相可见
- **立绘大小可调**：60%~160%，桌面与手机均实时生效
- 右上角：⚙ 设置 / 🎤 语音 / ✕ 退出 Galgame；设置面板与界面内开关双向联动，默认关闭

## 三、双端桌宠

- **安卓**：悬浮窗桌宠，9 种表情立绘随回复情绪切换；主动回复（默认关闭）；前台服务 + 开机自启 + 电池引导 + 无障碍保活
- **PC**：桌宠为**独立悬浮窗，与主界面共存**（不再二选一）；⟲ 一键激活主界面
- **桌宠语音输入**：点 🎤 说话 → 识别 → 直接发送（与主界面同一 ASR 链路）
- **尺度滑块 / 内容点击穿透 / 返回按钮**（安卓悬浮窗按钮可拉起主 App）

## 四、消息与语音

- **Markdown**：标题、列表、粗体、引用、代码块、表格、链接（离线渲染 + 安全转义）
- **LaTeX 公式**：行内与块级（KaTeX 离线，深色适配）
- **语音声纹进度**：长按语音条放大，已播部分实时点亮；按住拖动跳转，松开从目标位置继续播放；拖动后再点也能正常重播

## 五、稳定性与问题修复（两轮代码审查）

- **安卓**：悬浮窗 WebView 导航白名单、麦克风来源校验（修好桌宠语音）、输入框可弹出键盘（修好主线程问题）、拖动方向与边界钳制、悬浮窗类型 API 分支、权限预检 + 通知权限、模型跟随用户设置；无障碍跳过自身、停用即清空缓存
- **PC**：桌宠配置链路打通（此前设置读不到、桌宠聊天必失败）、云端语音识别修复（CORS 预检头）、代理路径白名单、多显示器/混合 DPI 定位、全局异常日志
- **前端**：桌宠开关失效修复、AI 换场景指令真正送达模型、代码块内示例不被误删、长按拖动被取消后不再卡死、立绘缩放修复
- **验证**：桌面 1440×900 / 手机 390×844 / 平板 820×1180 三设备共 28 项功能断言**全部通过**

## 六、安装与使用

| 平台 | 文件 | 说明 |
| --- | --- | --- |
| 安卓 | ElainaChat-1.3.1-release-signed.apk（50MB） | 直接安装，桌宠默认关闭 |
| PC | ElainaChat-PC-1.3.1-win64.zip（142.8MB） | 解压运行 ElainaPet.exe，免装 .NET |

1. 设置 → 云端连接：填写服务地址与使用 Key
2. 可选：外观切模板/深色；桌宠段开启桌宠（安卓首次按引导授权无障碍与悬浮窗）
3. 可选：Galgame 段开启界面，进入后右上角可随时退出

> 双仓库分别发布：正式版（私有）与开源版（公开，BYOK 自带服务与密钥），各自一个 Release 内含 PC 与安卓产物。
